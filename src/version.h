#pragma once

// RE2CC_VERSION is defined by the build from the VERSION file (see the Makefile).
// The fallback only applies to ad-hoc compiles that bypass it.
#ifndef RE2CC_VERSION
#define RE2CC_VERSION "0.0.0-dev"
#endif
